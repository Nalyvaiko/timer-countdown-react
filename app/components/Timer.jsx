const React = require('react');

const Timer = React.createClass({
    render: function () {
        return (
            <div>
                <h3>Timer.jsx rendered</h3>
            </div>
        );
    }
});

module.exports = Timer;
